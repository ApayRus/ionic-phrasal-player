import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vtt-phrasal-player',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
